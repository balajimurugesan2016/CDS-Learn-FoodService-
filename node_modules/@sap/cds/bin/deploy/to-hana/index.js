module.exports =  function deploy_to_hana (model, _kind, _dbSpecificParameter, {
    'no-save':no_save,
    'auto-undeploy': autoUndeploy = false,
    'tunnel-address': tunnelAddress
  }) {
    const deployHana = require('./hana')
    return deployHana (
        model,  _dbSpecificParameter,
        no_save, tunnelAddress,
        null, null, null,
        { autoUndeploy }
    )
}